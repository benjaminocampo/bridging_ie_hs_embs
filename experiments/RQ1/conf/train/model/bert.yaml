module: ${eval:TransformerModel}
params:
  batch_size: 32
  checkpoint_path: null
  pretrained_model_name_or_path: bert-base-uncased
  epochs: 4
  lr: 2e-5
  random_state: 42
  weight_decay: 0.01
  device: cpu
  num_labels: 2
  save_strategy: "no"